import 'package:flutter/material.dart';

class ListarUsuariosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(

      child: Center(
        child: Text(' Usuario Works '),
      ),
      
    );
  }
}