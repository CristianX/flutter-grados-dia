import 'package:flutter/material.dart';

class ListarClimaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(

      child: Center(
        child: Text(' Clima Works '),
      ),
      
    );
  }
}